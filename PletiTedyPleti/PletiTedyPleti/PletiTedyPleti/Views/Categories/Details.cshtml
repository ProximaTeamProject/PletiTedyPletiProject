@using System.Runtime.CompilerServices
@model PletiTedyPleti.Classes.CategoriesViewCombination

@{
    ViewBag.Categoties = "Details";
}

<h4>Категория</h4>

<div>
    <h2>@Html.DisplayFor(model => model.Category.name)</h2>

    @*<div class="row">
            @foreach (var post in ViewBag.Posts)
            {
                <div class="col-md-3">
                    <div>
                       @if(post.Category == Html.DisplayFor(model => model.name))
                        {
                        <a href="~/Views/Posts/Details/@post.Id">@post.name</a>
                        <div>
                            <img src="@Html.DisplayFor(model => model.adress)" />
                        </div>
                       }
                      </div>
                </div>
            }
        </div>*@

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Category.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Category.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Category.adress)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Category.adress)
        </dd>

    </dl>

    <div class="row">
        @foreach (var post in Model.PostsCollection)
        {
            <div class="col-md-3">
                <div>
                    <a href="/Posts/Details/@post.Id">@post.Title</a>

                    <div>

                        @if (Model.ImagesCollection.Any(x => x.name == post.Title))
                        {
                            <img src="@Model.ImagesCollection.FirstOrDefault(x=>x.name == post.Title).adress">

                        }

                    </div>

                </div>
            </div>
        }
    </div>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>